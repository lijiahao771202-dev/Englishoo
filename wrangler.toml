# Cloudflare Pages 配置文件
# 参考: https://developers.cloudflare.com/pages/functions/wrangler-configuration/

name = "english11"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

# Functions 目录配置 (默认为 ./functions)
# functions_directory = "functions"

[vars]
# 环境变量可以在这里配置，或者在 Cloudflare Dashboard 中配置
# DEEPSEEK_API_KEY = "your-api-key" # 建议在 Dashboard 中配置，更安全
